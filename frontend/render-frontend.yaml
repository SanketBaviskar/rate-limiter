services:
    # Frontend Static Site
    - type: web
      name: distributed-rate-limiter
      env: static
      rootDir: frontend
      buildCommand: npm install && npm run build
      staticPublishPath: dist
      envVars:
          - key: VITE_API_URL
      - key: VITE_API_URL
        value: placeholder_url_set_in_dashboard
